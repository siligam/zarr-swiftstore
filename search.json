{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"zarr-swiftstore","text":"<p>OpenStack Swift object storage backend for zarr v3. Enables direct read/write access to zarr datasets stored in Swift containers using <code>python-swiftclient</code> — no S3 compatibility layer required.</p>","path":["zarr-swiftstore"],"tags":[]},{"location":"#why-this-library-exists","level":2,"title":"Why this library exists","text":"<p>zarr v3 ships with <code>FsspecStore</code>, which supports many cloud backends (S3, GCS, Azure) through the fsspec ecosystem. For OpenStack Swift specifically, the standard recommendation is <code>swiftspec</code> + <code>FsspecStore</code>.</p> <p>However, many OpenStack deployments — particularly HPC and research clusters — expose Swift through TempAuth only, without a full Keystone identity service. In those environments:</p> <ul> <li>S3 compat (<code>swift3</code>/<code>s3api</code>) is unusable — signing S3 requests requires   EC2-style credentials that TempAuth deployments cannot issue without a Keystone   identity service.</li> <li><code>s3fs</code>/<code>boto3</code> do not work for the same reason.</li> <li><code>swiftspec</code> works in principle but has seen limited maintenance since 2022.</li> </ul> <p>This library uses <code>python-swiftclient</code> directly, which speaks the native Swift API and accepts pre-auth tokens. See ADR 001 for the full investigation.</p>","path":["zarr-swiftstore"],"tags":[]},{"location":"#quick-start","level":2,"title":"Quick start","text":"<pre><code>import zarr\nfrom zarrswift import SwiftStore\n\nstore = await SwiftStore.open(\n    container=\"my-container\",\n    prefix=\"zarr-demo\",\n    storage_options={\n        \"preauthurl\": \"https://swift.example.org/v1/AUTH_...\",\n        \"preauthtoken\": \"...\",\n    },\n)\n\nroot = zarr.open_group(store=store, mode=\"w\")\nz = root.zeros(\"data\", shape=(1000, 1000), chunks=(100, 100), dtype=\"f4\")\nz[:] = 3.14\n</code></pre>","path":["zarr-swiftstore"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#swiftstore","level":2,"title":"SwiftStore","text":"<pre><code>class SwiftStore(zarr.abc.store.Store)\n</code></pre> <p>zarr v3 storage backend for OpenStack Swift Object Storage.</p>","path":["API Reference"],"tags":[]},{"location":"api/#constructor","level":3,"title":"Constructor","text":"<pre><code>SwiftStore(\n    container: str,\n    prefix: str = \"\",\n    storage_options: dict | None = None,\n    *,\n    read_only: bool = False,\n)\n</code></pre> <p>Use <code>SwiftStore.open()</code> rather than the constructor directly — <code>open()</code> calls <code>_open()</code> which ensures the container exists.</p> <p>Parameters</p> Parameter Type Description <code>container</code> <code>str</code> Swift container name. Created on <code>open()</code> if it does not exist. <code>prefix</code> <code>str</code> Optional path prefix inside the container. <code>storage_options</code> <code>dict</code> Keyword arguments forwarded to <code>swiftclient.Connection</code>. <code>read_only</code> <code>bool</code> Open in read-only mode (default: <code>False</code>).","path":["API Reference"],"tags":[]},{"location":"api/#class-attributes","level":3,"title":"Class attributes","text":"Attribute Value <code>supports_writes</code> <code>True</code> <code>supports_deletes</code> <code>True</code> <code>supports_listing</code> <code>True</code>","path":["API Reference"],"tags":[]},{"location":"api/#methods","level":3,"title":"Methods","text":"","path":["API Reference"],"tags":[]},{"location":"api/#open-classmethod","level":4,"title":"<code>open</code> (classmethod)","text":"<pre><code>store = await SwiftStore.open(container, prefix=\"\", storage_options=None, read_only=False)\n</code></pre> <p>Open the store, creating the Swift container if it does not exist.</p>","path":["API Reference"],"tags":[]},{"location":"api/#get","level":4,"title":"<code>get</code>","text":"<pre><code>value = await store.get(key, prototype, byte_range=None)\n</code></pre> <p>Retrieve an object. Returns <code>None</code> if the key does not exist. Supports <code>RangeByteRequest</code>, <code>OffsetByteRequest</code>, and <code>SuffixByteRequest</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#set","level":4,"title":"<code>set</code>","text":"<pre><code>await store.set(key, value)\n</code></pre> <p>Write a buffer to Swift.</p>","path":["API Reference"],"tags":[]},{"location":"api/#delete","level":4,"title":"<code>delete</code>","text":"<pre><code>await store.delete(key)\n</code></pre> <p>Delete an object. Idempotent — does not raise if the key does not exist.</p>","path":["API Reference"],"tags":[]},{"location":"api/#exists","level":4,"title":"<code>exists</code>","text":"<pre><code>exists = await store.exists(key)\n</code></pre> <p>Return <code>True</code> if the key exists (uses a Swift HEAD request).</p>","path":["API Reference"],"tags":[]},{"location":"api/#list","level":4,"title":"<code>list</code>","text":"<pre><code>async for key in store.list():\n    ...\n</code></pre> <p>Iterate all keys in the store.</p>","path":["API Reference"],"tags":[]},{"location":"api/#list_prefix","level":4,"title":"<code>list_prefix</code>","text":"<pre><code>async for key in store.list_prefix(prefix):\n    ...\n</code></pre> <p>Iterate all keys under a prefix.</p>","path":["API Reference"],"tags":[]},{"location":"api/#list_dir","level":4,"title":"<code>list_dir</code>","text":"<pre><code>async for name in store.list_dir(prefix):\n    ...\n</code></pre> <p>Iterate one level of the virtual directory hierarchy (uses Swift <code>delimiter=\"/\"</code>).</p>","path":["API Reference"],"tags":[]},{"location":"api/#getsize","level":4,"title":"<code>getsize</code>","text":"<pre><code>size = await store.getsize(key)\n</code></pre> <p>Return the byte size of an object using a Swift HEAD request (no download). Raises <code>FileNotFoundError</code> if the key does not exist.</p>","path":["API Reference"],"tags":[]},{"location":"api/#with_read_only","level":4,"title":"<code>with_read_only</code>","text":"<pre><code>ro_store = store.with_read_only(read_only=True)\n</code></pre> <p>Return a new store instance with the same connection but different <code>read_only</code> flag.</p>","path":["API Reference"],"tags":[]},{"location":"api/#properties","level":3,"title":"Properties","text":"Property Type Description <code>url</code> <code>str</code> Public URL of the store root (<code>{swift_url}/{container}/{prefix}</code>). <code>container</code> <code>str</code> Container name. <code>prefix</code> <code>str</code> Path prefix (empty string if none). <code>storage_options</code> <code>dict</code> Connection options passed to <code>swiftclient.Connection</code>. <code>conn</code> <code>swiftclient.Connection</code> Live connection object.","path":["API Reference"],"tags":[]},{"location":"api/#zarrswiftutils","level":2,"title":"zarrswift.utils","text":"","path":["API Reference"],"tags":[]},{"location":"api/#acquire_token","level":3,"title":"<code>acquire_token</code>","text":"<pre><code>auth = acquire_token(authurl, user, key=None, update_env=True)\n</code></pre> <p>Authenticate via TempAuth v1.0 and return <code>{\"preauthurl\": ..., \"preauthtoken\": ...}</code>. If <code>update_env=True</code>, also sets <code>OS_STORAGE_URL</code> and <code>OS_AUTH_TOKEN</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#is_public","level":3,"title":"<code>is_public</code>","text":"<pre><code>public = is_public(store)\n</code></pre> <p>Return <code>True</code> if the container has <code>.r:*</code> in its <code>X-Container-Read</code> ACL.</p>","path":["API Reference"],"tags":[]},{"location":"api/#toggle_public","level":3,"title":"<code>toggle_public</code>","text":"<pre><code>toggle_public(store)\n</code></pre> <p>Add <code>.r:*,.rlistings</code> to the container ACL if absent; remove it if present.</p>","path":["API Reference"],"tags":[]},{"location":"api/#getenv_auth","level":3,"title":"<code>getenv_auth</code>","text":"<pre><code>auth = getenv_auth()\n</code></pre> <p>Read Swift credentials from environment variables (<code>OS_STORAGE_URL</code>, <code>OS_AUTH_TOKEN</code>, <code>ST_AUTH</code>, <code>ST_USER</code>, <code>ST_KEY</code>) and return a <code>storage_options</code> dict. Raises <code>ValueError</code> if no variables are set.</p>","path":["API Reference"],"tags":[]},{"location":"authentication/","level":1,"title":"Authentication","text":"<p>Credentials are passed as a <code>storage_options</code> dict to <code>SwiftStore.open()</code>. All keys are forwarded directly to <code>swiftclient.Connection</code>.</p>","path":["Authentication"],"tags":[]},{"location":"authentication/#pre-authenticated-token","level":2,"title":"Pre-authenticated token","text":"<p>The most common setup in TempAuth deployments. A token is obtained out-of-band (e.g. via <code>swift-token new</code> or <code>swift auth</code>) and passed directly:</p> <pre><code>import os\n\nstorage_options = {\n    \"preauthurl\": os.environ[\"OS_STORAGE_URL\"],\n    \"preauthtoken\": os.environ[\"OS_AUTH_TOKEN\"],\n}\n</code></pre> <p>Set the environment variables before opening a store:</p> <pre><code>export OS_STORAGE_URL=\"https://swift.example.org/v1/AUTH_...\"\nexport OS_AUTH_TOKEN=\"...\"\n</code></pre>","path":["Authentication"],"tags":[]},{"location":"authentication/#username-and-password-tempauth-v10","level":2,"title":"Username and password (TempAuth v1.0)","text":"<pre><code>storage_options = {\n    \"authurl\": \"https://swift.example.org/auth/v1.0\",\n    \"user\": \"{account}:{username}\",\n    \"key\": \"{password}\",\n}\n</code></pre>","path":["Authentication"],"tags":[]},{"location":"authentication/#acquiring-a-token-programmatically","level":2,"title":"Acquiring a token programmatically","text":"<p><code>zarrswift.utils.acquire_token</code> exchanges credentials for a pre-auth token and optionally sets <code>OS_STORAGE_URL</code> / <code>OS_AUTH_TOKEN</code> in the environment:</p> <pre><code>from zarrswift.utils import acquire_token\n\nauth = acquire_token(\n    authurl=\"https://swift.example.org/auth/v1.0\",\n    user=\"myaccount:myuser\",\n    key=\"mypassword\",\n    update_env=True,   # sets OS_STORAGE_URL and OS_AUTH_TOKEN\n)\n# auth = {\"preauthurl\": \"...\", \"preauthtoken\": \"...\"}\n</code></pre> <p>Note</p> <p><code>acquire_token</code> requires the optional <code>requests</code> dependency.</p>","path":["Authentication"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"","path":["Installation"],"tags":[]},{"location":"installation/#from-pypi","level":2,"title":"From PyPI","text":"<pre><code>pip install zarr-swiftstore\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#from-source","level":2,"title":"From source","text":"<p>With uv:</p> <pre><code>git clone https://github.com/siligam/zarr-swiftstore.git\ncd zarr-swiftstore\nuv sync\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Python 3.12+</li> <li><code>zarr &gt;= 3.0, &lt; 4</code></li> <li><code>python-swiftclient &gt;= 3.10, &lt; 5</code></li> </ul>","path":["Installation"],"tags":[]},{"location":"testing/","level":1,"title":"Running the Tests","text":"<p>Integration tests require a live Swift service. Enable them with <code>ZARR_TEST_SWIFT=1</code>.</p>","path":["Running the Tests"],"tags":[]},{"location":"testing/#against-a-local-swift-instance-docker","level":2,"title":"Against a local Swift instance (Docker)","text":"<p>The fastest way to run the full suite locally is with the openstackswift/saio Docker image, which provides a single-node Swift all-in-one:</p> <pre><code>docker run -d -p 8080:8080 openstackswift/saio\n\nexport ST_AUTH=http://localhost:8080/auth/v1.0\nexport ST_USER=test:tester\nexport ST_KEY=testing\nexport ZARR_TEST_SWIFT=1\n\npytest -v zarrswift/tests/test_storage.py\n</code></pre>","path":["Running the Tests"],"tags":[]},{"location":"testing/#against-a-remote-swift-service","level":2,"title":"Against a remote Swift service","text":"Pre-auth tokenUsername and password <pre><code>export OS_STORAGE_URL=\"https://swift.example.org/v1/AUTH_...\"\nexport OS_AUTH_TOKEN=\"...\"\nexport ZARR_TEST_SWIFT=1\n\npytest -v zarrswift\n</code></pre> <pre><code>export ST_AUTH=\"https://swift.example.org/auth/v1.0\"\nexport ST_USER=\"{account}:{user}\"\nexport ST_KEY=\"{password}\"\nexport ZARR_TEST_SWIFT=1\n\npytest -v zarrswift\n</code></pre>","path":["Running the Tests"],"tags":[]},{"location":"testing/#expected-results","level":2,"title":"Expected results","text":"<p>The test suite runs zarr v3's full <code>StoreTests</code> class (75 tests). Five sync-API tests are automatically skipped — they are not applicable to async stores:</p> <pre><code>70 passed, 5 skipped\n</code></pre>","path":["Running the Tests"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/","level":1,"title":"ADR 001: Use native Swift API rather than fsspec S3 compatibility","text":"<p>Date: 2026-05-11 Status: Accepted</p>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#context","level":2,"title":"Context","text":"<p>zarr v3 ships with <code>FsspecStore</code>, which supports many cloud backends through the fsspec ecosystem. For OpenStack Swift specifically, two generic alternatives exist:</p> <ul> <li><code>swiftspec</code> + <code>FsspecStore</code> — an fsspec driver for Swift maintained under the   fsspec organisation.</li> <li><code>s3fs</code> / <code>boto3</code> + <code>FsspecStore</code> — via OpenStack's S3 compatibility middleware   (<code>swift3</code> / <code>s3api</code>), which exposes Swift containers as S3 buckets.</li> </ul> <p>We investigated whether either alternative makes zarr-swiftstore redundant.</p>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#investigation","level":2,"title":"Investigation","text":"","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#swiftspec","level":3,"title":"swiftspec","text":"<p><code>swiftspec</code> provides an fsspec-compatible Swift driver and is the recommended generic path for zarr v3 + Swift. It is a valid option for deployments where Keystone is available and credentials are straightforward.</p> <p>However, <code>swiftspec</code> has seen limited maintenance activity since 2022. For zarr v3 users who need an actively maintained, tested integration, this is a concern.</p>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#s3-compatibility-middleware","level":3,"title":"S3 compatibility middleware","text":"<p>Many OpenStack deployments run <code>swift3</code> or <code>s3api</code> middleware, which exposes the Swift API as an S3-compatible endpoint. If usable, this would allow standard <code>s3fs</code>/<code>boto3</code> tooling to work without any Swift-specific library.</p> <p>The critical limitation: S3 compatibility requires signing requests with AWS-style HMAC credentials (an access key + secret key pair). These come from one of two sources:</p> <ol> <li>Keystone EC2 credentials — generated via <code>openstack ec2 credentials create</code>.    Requires a full Keystone identity service.</li> <li>TempAuth credentials — the TempAuth username and password used directly as    the S3 access key and secret key.</li> </ol> <p>In practice, many HPC and research cluster deployments use TempAuth only, without a Keystone identity service. These deployments typically distribute pre-authenticated tokens (<code>OS_AUTH_TOKEN</code> + <code>OS_STORAGE_URL</code>) rather than long-lived username/password pairs.</p> <p>We tested S3 compat access on such a deployment. Results:</p> <ul> <li>The S3 middleware was confirmed active (requests received well-formed XML S3   error responses).</li> <li>All credential formats tried (<code>account:user</code> / password, username / password)   returned <code>403 SignatureDoesNotMatch</code>.</li> <li>A request signed with a completely fabricated key returned the same   <code>403 SignatureDoesNotMatch</code> — the middleware does not distinguish between   \"key not found\" and \"wrong signature\", a common security hardening practice.</li> <li>No EC2 credential endpoint was reachable (Keystone not running).</li> <li>The OpenStack CLI (<code>openstack ec2 credentials create</code>) was not available.</li> </ul> <p>Conclusion: On TempAuth-only deployments, the S3 compatibility middleware is present but not accessible to end users. The TempAuth user database is not wired into the S3 auth backend, and without Keystone there is no mechanism to generate EC2 credentials. <code>s3fs</code> and <code>boto3</code> do not work.</p>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#decision","level":2,"title":"Decision","text":"<p>Maintain zarr-swiftstore as a native Swift API backend using <code>python-swiftclient</code>.</p> <p><code>python-swiftclient</code> accepts both pre-authenticated tokens and TempAuth v1.0 username/password credentials directly, requiring no S3 compatibility layer. This is the only reliable path for zarr v3 users on TempAuth-only OpenStack deployments.</p>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"adr/001-native-swift-over-s3-compat/#consequences","level":2,"title":"Consequences","text":"<ul> <li>zarr-swiftstore remains a dependency for users on TempAuth-only deployments.</li> <li>Users on full Keystone deployments with EC2 credentials available may prefer   <code>zarr.storage.FsspecStore</code> with <code>swiftspec</code> or <code>s3fs</code> — this library does not   compete with that path.</li> <li>The <code>zarrswift.utils</code> module (<code>acquire_token</code>, <code>toggle_public</code>, <code>is_public</code>)   provides Swift-native utilities that have no equivalent in the fsspec/S3 path.</li> </ul>","path":["Architecture","ADR 001: Use native Swift API rather than fsspec S3 compatibility"],"tags":[]},{"location":"usage/utilities/","level":1,"title":"Container Utilities","text":"<p><code>zarrswift.utils</code> provides Swift-native helpers for container access control.</p>","path":["Usage","Container Utilities"],"tags":[]},{"location":"usage/utilities/#checking-public-access","level":2,"title":"Checking public access","text":"<pre><code>from zarrswift import SwiftStore\nfrom zarrswift.utils import is_public\n\nstore = await SwiftStore.open(\"my-container\", storage_options=storage_options)\nprint(is_public(store))   # False\n</code></pre>","path":["Usage","Container Utilities"],"tags":[]},{"location":"usage/utilities/#toggling-public-read-access","level":2,"title":"Toggling public read access","text":"<pre><code>from zarrswift.utils import toggle_public\n\ntoggle_public(store)      # make public\nprint(is_public(store))   # True\n\ntoggle_public(store)      # make private again\nprint(is_public(store))   # False\n</code></pre> <p>Making a container public sets <code>X-Container-Read: .r:*,.rlistings</code>, which allows unauthenticated HTTP GET access to all objects and listings. Toggling back removes the ACL entry.</p> <p>Warning</p> <p>Public containers are readable by anyone with the URL. Only use this for datasets you intend to share openly.</p>","path":["Usage","Container Utilities"],"tags":[]},{"location":"usage/utilities/#acquiring-a-token","level":2,"title":"Acquiring a token","text":"<pre><code>from zarrswift.utils import acquire_token\n\nauth = acquire_token(\n    authurl=\"https://swift.example.org/auth/v1.0\",\n    user=\"myaccount:myuser\",\n    key=\"mypassword\",\n)\n# Returns {\"preauthurl\": \"...\", \"preauthtoken\": \"...\"}\n# Also sets OS_STORAGE_URL and OS_AUTH_TOKEN in the environment by default\n</code></pre>","path":["Usage","Container Utilities"],"tags":[]},{"location":"usage/xarray/","level":1,"title":"Using with xarray","text":"","path":["Usage","Using with xarray"],"tags":[]},{"location":"usage/xarray/#writing-a-dataset","level":2,"title":"Writing a dataset","text":"<pre><code>import numpy as np\nimport xarray as xr\nfrom zarrswift import SwiftStore\n\nstorage_options = {\n    \"preauthurl\": \"https://swift.example.org/v1/AUTH_...\",\n    \"preauthtoken\": \"...\",\n}\n\nstore = await SwiftStore.open(\n    container=\"my-container\",\n    prefix=\"xarray-demo\",\n    storage_options=storage_options,\n)\n\nds = xr.Dataset(\n    {\"temperature\": ((\"time\", \"lat\", \"lon\"), np.random.rand(12, 180, 360))},\n    coords={\n        \"time\": np.arange(12),\n        \"lat\": np.linspace(-90, 90, 180),\n        \"lon\": np.linspace(-180, 180, 360),\n    },\n)\nds.to_zarr(store=store, mode=\"w\", consolidated=True)\n</code></pre>","path":["Usage","Using with xarray"],"tags":[]},{"location":"usage/xarray/#reading-a-dataset","level":2,"title":"Reading a dataset","text":"<pre><code>store = await SwiftStore.open(\n    container=\"my-container\",\n    prefix=\"xarray-demo\",\n    storage_options=storage_options,\n)\n\nds = xr.open_zarr(store=store, consolidated=True)\nprint(ds)\n</code></pre> <p>Tip</p> <p>Use <code>consolidated=True</code> for large datasets — it caches all metadata in a single <code>.zmetadata</code> object, avoiding many small HTTP requests on open.</p>","path":["Usage","Using with xarray"],"tags":[]},{"location":"usage/zarr/","level":1,"title":"Using with zarr","text":"","path":["Usage","Using with zarr"],"tags":[]},{"location":"usage/zarr/#opening-a-store","level":2,"title":"Opening a store","text":"<pre><code>import zarr\nfrom zarrswift import SwiftStore\n\nstorage_options = {\n    \"preauthurl\": \"https://swift.example.org/v1/AUTH_...\",\n    \"preauthtoken\": \"...\",\n}\n\nstore = await SwiftStore.open(\n    container=\"my-container\",\n    prefix=\"zarr-demo\",\n    storage_options=storage_options,\n)\n</code></pre> <p>The <code>container</code> is created automatically if it does not exist.</p>","path":["Usage","Using with zarr"],"tags":[]},{"location":"usage/zarr/#writing-arrays","level":2,"title":"Writing arrays","text":"<pre><code>root = zarr.open_group(store=store, mode=\"w\")\n\n# Create and write an array\nz = root.zeros(\"data\", shape=(1000, 1000), chunks=(100, 100), dtype=\"f4\")\nz[:] = 3.14\n</code></pre>","path":["Usage","Using with zarr"],"tags":[]},{"location":"usage/zarr/#reading-arrays","level":2,"title":"Reading arrays","text":"<pre><code>root = zarr.open_group(store=store, mode=\"r\")\nz = root[\"data\"]\nprint(z[:10, :10])\n</code></pre>","path":["Usage","Using with zarr"],"tags":[]},{"location":"usage/zarr/#read-only-mode","level":2,"title":"Read-only mode","text":"<pre><code>store = await SwiftStore.open(\n    container=\"my-container\",\n    prefix=\"zarr-demo\",\n    storage_options=storage_options,\n    read_only=True,\n)\n</code></pre>","path":["Usage","Using with zarr"],"tags":[]},{"location":"usage/zarr/#using-as-a-context-manager","level":2,"title":"Using as a context manager","text":"<pre><code>async with await SwiftStore.open(\"my-container\", storage_options=storage_options) as store:\n    root = zarr.open_group(store=store, mode=\"w\")\n    root.zeros(\"data\", shape=(100,), dtype=\"i4\")\n</code></pre>","path":["Usage","Using with zarr"],"tags":[]}]}